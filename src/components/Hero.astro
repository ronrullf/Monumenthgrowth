---
import Section from './Section.astro';
import Eyebrow from './Eyebrow.astro';
import LayerPanel from './LayerPanel.astro';
import WhatsAppButton from './WhatsAppButton.astro';
import { MESSAGES } from '../lib/whatsapp';
---
<Section id="hero" tone="canvas" class="pt-8 md:pt-12">
  <div class="grid gap-10 lg:grid-cols-12 lg:items-center">
    <div class="lg:col-span-7 reveal">
      <Eyebrow text="FOR KITCHEN & BATH REMODELERS" />
      <h1 id="hero-heading" class="mt-4">
        Book more <span class="italic">clients</span>.
        Just 1–2 hours a month.
      </h1>
      <p class="mt-6 text-lg leading-relaxed text-foreground max-w-xl">
        You film the job. We turn it into booked leads.
      </p>
      <ul class="mt-5 grid grid-cols-1 sm:grid-cols-2 gap-x-6 gap-y-2.5 max-w-xl" data-reveal-group>
        {[
          'Your footage becomes short-form content.',
          "Your website's wired to WhatsApp.",
          'Your follow-ups never go cold.',
          'Built only for remodelers like you.',
        ].map((item) => (
          <li class="reveal flex items-start gap-2.5 text-base leading-snug text-muted-foreground">
            <span class="mt-2 h-1 w-1 flex-shrink-0 bg-foreground" aria-hidden="true"></span>
            {item}
          </li>
        ))}
      </ul>
      <div class="mt-8">
        <WhatsAppButton message={MESSAGES.hero} label="Get in touch" variant="primary" />
      </div>
      <p class="mt-4 text-sm text-muted-foreground">
        No forms. No email chains. One WhatsApp conversation.
      </p>
    </div>

    <div class="lg:col-span-5 reveal" style="transition-delay: 120ms;">
      <LayerPanel elevation="lg" class="max-w-md mx-auto lg:mx-0">
        <div class="aspect-[4/5] flex items-center justify-center p-8">
          <div class="flex flex-col items-center gap-4 text-muted-foreground">
            <span
              class="flex h-16 w-16 items-center justify-center rounded-full bg-muted"
              aria-hidden="true"
            >
              <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
                <path d="M8 5v14l11-7z" />
              </svg>
            </span>
            <span class="text-sm text-center">Your raw footage goes here</span>
          </div>
        </div>
      </LayerPanel>
    </div>
  </div>
</Section>
